const port = 3000
const url = 'http://localhost:' + port

const database_credentials = {
    database_host: 'localhost',
    user: 'root',
    password: '',
    database: 'pirmoji_duombaze'
}

export {port, url}

export default database_credentials
